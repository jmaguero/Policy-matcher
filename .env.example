ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-OpenAI-api-key
# If running in Docker, use http://host.docker.internal:11434 (Mac/Windows)
# or http://172.17.0.1:11434 (Linux default bridge) or your LAN IP.
# Do NOT use localhost:11434 as that refers to the container itself.
OLLAMA_HOST=http://x.x.x.x:11434

# DO NOT add SERVE_FRONTEND here. Set it inline on the command line for local
# dev only (see README Option 2). Adding it here would break the Docker build.
