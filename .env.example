<<<<<<< HEAD
# ── LLM provider keys ─────────────────────────────────────────────────────────
# Required when using Anthropic models (claude-haiku-4-5, claude-sonnet-4-5)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Required when using OpenAI models (gpt-4.1, gpt-5.2)
OPENAI_API_KEY=your-openai-api-key

# Required when using Ollama (local LAN address of the machine running Ollama)
OLLAMA_HOST=http://192.168.x.x:11434

# ── Report template ───────────────────────────────────────────────────────────
# Path to the .docx template used by Button 3 to generate the compliance report.
# Docker Compose sets this automatically to /app/documents/template_report.docx.
# Override only when running the backend locally outside Docker.
# Default (local dev): ./documents/template_report.docx  (auto-resolved, no override needed)
# TEMPLATE_PATH=/absolute/path/to/template_report.docx
=======
ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-OpenAI-api-key
OLLAMA_HOST=http://192.168.x.x:11434
>>>>>>> origin/main
